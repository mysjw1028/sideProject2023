<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.ruise.firstapp.domain.member.memberDao">

    <insert id="insert" useGeneratedKeys="true" keyProperty="memberId"> 
        INSERT INTO
        member(member_password, member_tel, member_jumin, mamber_gender, member_nick_name,member_post_code, member_adress1, member_adress2, member_state, create_date)
        VALUES(#{memberPassword}, #{memberTel}, #{memberJumin}, #{memberGender}, #{memberNickName},
        #{memberPostCode}, #{memberAdress1}, #{memberAdress2}, #{memberState}, NOW())
    </insert>

    <select id="findById" resultType="site.ruise.firstapp.domain.member.member"> 
        SELECT * FROM
        member WHERE member_id = #{memberId} 
    </select>
</mapper>
    